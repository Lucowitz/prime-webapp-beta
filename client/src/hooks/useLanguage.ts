// Import directly from the context
import { useLanguage as useLanguageContext } from "../context/LanguageContext";

// Export the hook directly from context
export const useLanguage = useLanguageContext;
